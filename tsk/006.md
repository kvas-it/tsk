---
status: done
created: 2026-03-20
tags: [bug, cli]
---

# Fix hardcoded digit width in tsk list

`cmd_list` uses `{:0>3}` instead of the configured digit width from
`tsk.yaml`. Should use `config.digits` like the rest of the code does.
