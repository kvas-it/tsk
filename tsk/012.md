---
status: done
created: 2026-03-20
tags: [cli, fix]
---

# Skip editor in non-interactive mode

`tsk new` was opening `$EDITOR` even when run from scripts or AI agents.
Now checks `stdin.is_terminal()` and only opens the editor interactively.
Otherwise just prints the filename.
